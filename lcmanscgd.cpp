#include<iostream>
using namespace std;
int main(){

    // int num1, num2;
    // cin >> num1 >> num2;
    // int temp1 = num1;
    // int temp2 = num2;
    // while (num1 % num2 != 0)
    // {
    //   int remainder = num1 % num2;
    //   num1 = num2;
    //   num2 = remainder;
    // }
    // int gcd = num2;
    // int lcm = (temp1 * temp2) / gcd;
    // cout<<(gcd)<<endl;
    // cout<<(lcm);
    // return 0;
    int n;
    cin >> n;

    for (int div = 2; div <= n; div++)
    {
      while (n % div == 0)
      {
        cout<<(div + " ");
        n = n / div;
      }
    }
    if (n != 1)
    {
      cout<<(n);
    }
}


































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































